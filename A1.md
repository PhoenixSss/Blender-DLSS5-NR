# A1 — Native Feature 18 Probe

状态：**通过验收**（2026-09-05，RTX 5090 + 驱动 616.64）

独立于 Blender 的验证工具：固定图像 → Feature18Backend → 输出图像 + JSON 报告，
证明 runtime → D3D12 → NGX → Feature 18 全链路可用（需求书 §18/§19）。

## 构建

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File build.ps1
```

产物（`bin\`）：
- `dlss5nr_probe.exe` — probe CLI
- `nvngx.dll_dlss5.dll` — caller shim（**/Od 强制**，见下）
- `dlss5nr_unit_tests.exe` — 39 项 CPU 单元测试

构建脚本自动发现 MSVC（含 D 盘安装）与 Windows SDK；本机命中 VS18 MSVC 14.50.35717
+ SDK 10.0.26100.0。脚本**绝不复制/下载 NVIDIA DLL**（§3/§30）。

## 运行（本机黄金命令）

```powershell
.\bin\dlss5nr_probe.exe --runtime "D:\workspace\program\DLSS5-Blender\DLSS.5.Visual.Enhancer.v5.0\bin\runtime\host"
```

输出：`bin\a1_out.input.png` / `bin\a1_out.png` / `bin\a1_out.rgba_f32.bin` /
`bin\a1_out.report.json`。退出码：0 成功；2 CLI 用法错；`10+错误类别`（§25）。

常用参数：`--input <png>`、`--width/--height`、`--gpu <n>`、`--ngx-core <path>`、
`--style/--preset/--intensity/--tone/--structure/--skin/--auto-mask`、
`--encoding <scene-linear|standard|agx|proxy>`、`--reject-unsigned`、`--backend <name>`。

## 本机实测记录

| 项目 | 值 |
|---|---|
| GPU / 检测 | RTX 5090，NVML 架构枚举 10 = Blackwell，策略 OK |
| 驱动 | 616.64 |
| runtime | `nvngx_dlssnr.dll` 165,830,144 B，FileVersion **310.8.2.0**，SHA-256 `6eb209e7…3927` |
| runtime 策略 | 无 Authenticode → 分类 `unknown`，**警告 + 继续**（§17 字面规则，非拒绝类） |
| NGX core | DriverStore `nv_dispi.inf_amd64_436833bbb0f00476\_nvngx.dll` |
| CreateFeature / EvaluateFeature | 1 / 1（NGX_SUCCESS），单次约 250 ms，全程约 1.5 s |
| 确定性 | 多次运行 output_sha256 逐位一致（`e953bf7d…407e`，Reset=1、无 RNG） |
| 失败演练 | `--runtime C:\nope` → EXIT=13，error JSON 含 category/stage/raw_code/gpu/driver |

## 关键工程发现（A2+ 必须知道）

1. **Caller shim 模块名是校验的一部分**：snippet runtime 的 caller 校验
   （0xBAD00002）要求调用模块名匹配 `nvngx*.dll` 模式。shim 名为
   `nvngx.dll_dlss5.dll`（避免与真正的 app 侧 loader 名 `nvngx.dll` 冲突）。
2. **/Od 是双保险**：实测 /O2 构建也能工作（`FinishCall` 的 volatile 后置存储
   使尾调用优化不可能发生，反汇编确认仍是 `call`），但 build.ps1 强制 /Od。
3. **FreeLibrary(nvngx_dlssnr.dll) 会永久挂起**（成功 Evaluate 后其
   DLL_PROCESS_DETACH 阻塞）→ NGX 模块按进程生命周期持有，不卸载。
   probe 和 Blender 插件均为进程内常驻，安全；官方 API 出现后再评估卸载路径。
4. ReadFile 在部分文件系统上 EOF 时返回 FALSE + GetLastError()==0（非
   ERROR_HANDLE_EOF）→ SHA-256 分块读取按实际读取字节数判 EOF。
5. 反汇编工具：`dumpbin` 需 `MSYS_NO_PATHCONV=1`，位于 MSVC `bin\Hostx64\x64\`。

## 验收清单（映射需求书 §29）

- Installation：N/A（A1 无 ZIP；probe 为独立 exe + 自有 shim，不含 NVIDIA runtime）✓
- GPU：RTX 5090 通过策略后走真实 capability 链 ✓；旧 GPU 拒绝路径由单测覆盖
  （RTX 4090+arch9 → 拒绝；RTX 50 名+旧 arch → 拒绝）✓；无 compatibility mode ✓
- Runtime：用户指定路径 ✓；version/hash/签名可见 ✓；known-modified 黑名单机制
  就位（A1 为空）✓；CreateFeature 失败不会误报成功 ✓（0xBAD00002 实测路径）
- Image Processing：Combined→Feature18→输出 等价路径成功 ✓；同分辨率 ✓；
  输出非空 ✓；Evaluate success ✓；同环境结果稳定（哈希逐位一致）✓；
  alpha 原样保留（§14 完整语义留给 A4）✓；无临时文件/FFmpeg/视频 ✓
- Architecture：隔离 grep 仅 1 处命中（CLI 帮助文本中的用户可见文件名，
  §8 要求，记录为例外）；canonical/probe/diagnostics/interface 无任何
  DLSSNR 参数名/feature ID/shim 概念 ✓
- Diagnostics：失败时报告 GPU/driver/runtime/hash/签名/stage/raw error ✓
- 明确未实现（§9/§12）：MV/Depth/ControlMask 未绑定（代码中仅
  Color/Output/Reset + 元数据）✓；无放大（ScalingRatio=1）✓；无旧 GPU 代码
  （无 SF/retarget/fatbin 等）✓

## A1 边界与已知限制

- 上传钳制 [0,1]、alpha 强制 1（参考实现已知良好路径）；HDR 与 alpha 语义
  在 A3/A4 处理。
- `--encoding` 目前仅记录元数据，不改变像素（A3 实现转换）。
- 通道序（RGB/BGR）由 backend 对照输入自动判定，判定逻辑在 feature18 内。
- 单线程串行（§26 互斥锁）；probe 一次处理一张图。
