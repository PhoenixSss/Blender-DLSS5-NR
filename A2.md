# A2 — Blender Bridge Integration（通过验收）

状态：**已验收**（2026-09-05，用户视觉确认）。headless 全链路 + GUI 图像路径均通过，用户已验证生成图像。Render Result 路径为可选补充验证。

数据路径：`Blender Image/Render Result → ctypes → nr_bridge.dll → Feature18Backend → DLSS5_NR_Result Image datablock`

## 交付内容

| 文件 | 职责 |
|---|---|
| `native/nr_bridge.h` / `nr_bridge.cpp` | §15 C ABI（nr_create/initialize/evaluate/get_diagnostics/destroy + NR_Error/NR_Diagnostics）；仅依赖接口层类型 |
| `bin/nr_bridge.dll` | 桥接 DLL（build.ps1 目标 [4/4]） |
| `blender/bridge.py` | ctypes 封装：DLL 发现（`DLSS5NR_NATIVE_DIR` → `../bin/`）、结构体镜像、NRBridgeError |
| `blender/render_result.py` | 输入提取：Image datablock（bottom-up→top-left 翻转）；Render Result 经 compositor Viewer Node（GUI 路径） |
| `blender/image_output.py` | 输出 `DLSS5_NR_Result` float Image（top-left→bottom-up 翻转，无临时文件 §29） |
| `blender/addon/__init__.py` | A2 最小 operator：Process Image Datablock / Process Render Result（UI 面板属 A7） |
| `blender/a2_test.py` | headless 测试脚本（load → bridge → save + 哈希打印） |

## 验收结果（headless，RTX 5090 + 616.64 + Blender 5.2.1）

| 项 | 结果 |
|---|---|
| 全链路（12140.png 3840×2160） | ✅ CreateFeature=1、EvaluateFeature=1、Image datablock 写出 |
| **Cross-validation** | ✅ **输出哈希与 probe 逐位一致**：`2843dd23…6388da`（Blender == probe） |
| 翻转正确性 | ✅ 写入→读回往返逐位一致；Blender row0 == canonical 末行（bottom-up 确认） |
| 失败演练 | ✅ `--runtime C:\nope` → `category=RuntimeMissing stage=runtime_identity raw=0x00000003 …gpu=RTX 5090 driver=616.64` |
| 隔离 grep | ✅ native/、blender/ 无 raw ABI 泄漏（仅 §8 要求的用户可见文件名例外） |
| 单元测试回归 | ✅ 39/39 |

**输入哈希差异说明**：Blender 输入 f32 哈希（`b3362b79…`）≠ probe（`66f08ee4…`）——OIIO 与 stb 的 8-bit→f32 舍入路径不同（尾位级）。backend 上传时经 f16 量化，差异消失，故输出逐位一致。此差异无害，已记录。

## Blender 5.x 关键 API 事实（实现约束）

1. **Render Result 图像不可 Python 读取**（5.0 起 size=0）→ 产品路径经 compositor **Viewer Node**（`Scene.compositing_node_group` 内 RLayers→Viewer），且 viewer 数据需 GUI 下合成工作区显示后才更新。
2. `Scene.node_tree` 已移除，新入口 `Scene.compositing_node_group`；`use_nodes` 弃用。
3. headless 下部分 compositor 操作不可靠（本 Steam 版实测），故 Render Result 路径只能 GUI 验收。

## 已知限制

- **PNG 文件保存经过 Blender 色彩管理**（float 图像被当作 scene-linear 重新编码）——debug 用 PNG 与 probe 的 PNG 像素不逐位一致属预期；**产品数据路径是 Image datablock 的 float buffer（哈希已验证）**，A3 色彩阶段统一处理编码语义。
- bridge 的 shim 按"本模块目录"定位（`GetModuleHandleEx(FROM_ADDRESS)`），probe（exe）与 bridge（DLL）两场景均正确。
- 4K 图单次评估约 1-2 s（A2 可接受；A7 优化）。

## GUI 验收记录（用户执行，2026-09-05）

- ✅ **图像路径通过**：控制台 `exec` 加载 addon → `bpy.ops.dlss5nr.process_image()`（经 `blender/gui_test.py` 的 context override）→ `DLSS5_NR_Result` 3840×2160 float 写出，诊断打印完整（GPU/runtime hash/class）。
- ⚠️ F3 搜索暂搜不到自定义 operator（Blender 5.x 搜索索引行为）——不影响功能，A7 做正式 UI 时解决。
- Render Result 路径（可选实测）：F12 渲染 → 顶部切到 Compositing 工作区 → 控制台 `bpy.ops.dlss5nr.process_render_result()`。若 Viewer 为空会有明确报错提示。
