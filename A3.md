# A3 — SceneLinear / Standard / AgX 色彩对比实验

状态：**已验收**（2026-09-05，用户视觉测试）。

**验收结论：产品默认输入编码 = AgX（AgXDisplay）**。观察记录：AgX 饱和度比另外两种略低，但**无过曝问题**（强高光场景下表现最稳定）——与"显示映射输入最接近游戏原生工作域"的设计结论一致。该选择已落实：scene 驱动的 auto 模式中 AgX 为产品默认（AgX 视图→AgX；未知/其他视图→AgX；Filmic 场景→AgX 并提示 Filmic 推迟；Raw/False Color→SceneLinear+钳制；Standard→Standard）。

## 三条路径（§13 A/B/C）

所有路径的输入都是**原始 scene-linear 数据**（合成器接口即未映射线性值——视图变换只发生在显示端，不发生在合成器缓冲区；实测场景最高值 ~301 即为证据）。转换在 `canonical/color_transform.{h,cpp}` 实现，probe 与 bridge **共享同一实现** → 两端 cross-validation 逐位一致。

| 路径 | 值 | 输出域定义 | 标签 |
|---|---|---|---|
| A. SceneLinear | 0 | scene-linear（钳制 [0,1]） | Linear Rec.709 |
| B. StandardDisplay | 1 | sRGB(scene-linear) — sRGB EOTF | sRGB |
| C. AgXDisplay | 2 | sRGB(AgxToneMap(scene-linear)) — AgX 色调映射 + sRGB 编码 | sRGB |

（D. ExperimentalProxy 未实现，仅当 A/B/C 均失败再做。）

## 关键结论

1. **网络需要的是有界数据，不是特定映射**。实测：无界 HDR 直通（最高 ~301）触发局部块状伪影；钳制的 Linear Rec.709（A1/A2 基线）与显示映射路径均正常。已知良好输入域为 [0,1]。
2. **产品输入域 = 显示映射数据**（用户决策 2026-09-05）：游戏引擎喂给 DLSS 的是玩家看到的最终画面（色调映射后、显示编码的值），网络权重在该域校准——显示映射输入最接近其设计工作模式。A（钳制线性）保留为 Raw/False Color 场景路径与实验选项。
3. **默认钳制 ON**（backend / probe / bridge / addon 全链路默认；`--no-clamp` 仅限实验）。默认回归已验证：probe 默认输出 = A2 基线 `2843dd23…` 逐位一致。
4. **默认编码由场景驱动**：读取 `scene.view_settings.view_transform` 自动选择（见下表）。HDR 策略随之统一——显示变换天然把 >1 值压缩进 [0,1]，无需硬钳制"处理"HDR。

| 场景视图变换 | 输入编码 |
|---|---|
| AgX | AgXDisplay (2) |
| Standard | StandardDisplay (1) |
| Filmic / Filmic Log | AgXDisplay (2)（产品默认；Filmic 支持推迟，见下） |
| Raw / False Color | SceneLinear (0) + clamp |
| 其他/未知 | AgXDisplay (2)（产品默认） |

## 验证记录

| 项 | 结果 |
|---|---|
| 单元测试 | ✅ 1109 项（sRGB 已知值/单调性；AgX 端点/中灰≈0.22 中性/单调/NaN 安全/确定性；`SrgbEotf∘AgxToneMap` 组合断言） |
| 回归基线 | ✅ probe 默认输出 = A2 基线逐位一致 |
| HDR 三路径 | ✅ 10×/100× 高光合成图全部成功、无 NaN、确定性一致 |
| EXR bit-exact 桥梁 | ✅ Blender 存 EXR → probe 输入哈希与 Blender 内处理一致；HDR cross-validation 通道建立 |
| 隔离 grep | ✅ 无新增泄漏 |

## 用户实测发现的两个 Bug（均已修复）

1. **HDR 直通伪影**：渲染含 scene-linear 值最高 ~301；clamp=False 时 SceneLinear/Standard 路径出现彩色块状伪影（画框区域），钳制后消失；AgX 内部有界故稳定。→ 默认钳制恢复 ON。
2. **AgX 输出域误标**：`AgxToneMap`（three.js/Filament 实现）输出**线性** tone-mapped 值（源码注释 "outputs are encoded as Linear-sRGB"），被标记为 sRGB → Blender 对线性数据再做 sRGB 解码 + 视图变换 → 重复映射、异常变暗。→ `AgXDisplay` 改为 `SrgbEotf(AgxToneMap(x))`，严格符合 §13 "AgX view → display/sRGB representation"。
3. **"Linear" 不是 Blender 5.2 的有效色彩空间名**（`scene_linear` 仅是角色别名）→ 改用规范名 **Linear Rec.709**，并对无效名加回退保护。

## AgX 实现说明

- 系数移植自 three.js（MIT）/ Google Filament（Apache-2.0）AgX（经典闭式多项式 + 矩阵，见 THIRD_PARTY_NOTICES）。单测验证灰度保持（矩阵行和≈1）。
- Blender 5.2 查看器的 AgX 是 LUT 变体（ACES2065→E-Gamut + lg2 分配 + `AgX_Base_sRGB.cube`），与闭式经典 AgX 在极端高光处有差异——本实验需要的是"AgX 家族的显示映射形状"，不需要与查看器逐位一致。

## Filmic 支持（已推迟，用户决策 2026-09-05）

场景视图为 Filmic 时暂映射到 Standard 并提示。后续实现二选一（许可证上均可行）：

- **闭式 Duiker 曲线**（自实现数学，probe/bridge 共享，与 LUT 版有细微差异）——推荐用于 A3 阶段的网络输入域实验；
- **运行时读用户 Blender 安装的 LUT**（不随项目分发 GPL 数据，与查看器逐位一致；probe 以 `--filmic-lut <path>` 参数保持对等验证）。

**许可证澄清**：Filmic 无闭源问题——Blender/filmic-blender 均为 GPL，约束仅在于 MIT 项目不能**再分发** GPL 数据文件；数学自实现或运行时读用户数据均完全合法（与用户自备 runtime DLL 同一政策逻辑）。

## 视觉验收记录（用户执行，2026-09-05）

- 测试场景：`场景打包.blend`（Cycles 2160×1080，Filmic 视图，含灯泡/金属等强高光）。
- **结论：产品默认 = AgX**。AgX 饱和度略低，但无过曝、高光稳定；SceneLinear+钳制高光被硬切（结构丢失）；Standard 无突出优势。
- 画框伪影问题确认已修复（默认钳制 + AgX 输出域修复）。

## 查看提示

B/C 输出是显示编码数据，Blender 查看器按标准管线（解码 → 场景视图变换）显示，输入与输出同等待遇、对比公平；观察网络在 C 域的原始输出可将查看器视图切到 Raw/Standard。
