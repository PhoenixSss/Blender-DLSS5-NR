# A3 — SceneLinear / Standard / AgX 色彩对比实验

状态：**实现与自动验证完成**（2026-09-05）；**默认编码待用户视觉验收后决策**（§13：默认模式必须依据测试结果）。

## 三条路径（§13 A/B/C）

| 路径 | CLI/API 值 | 实现 |
|---|---|---|
| A. SceneLinear | `scene-linear` / 0 | 恒等（scene-linear HDR 直通，NaN 消毒）；**默认不钳制 [0,1]**（clamp 为显式开关） |
| B. StandardDisplay | `standard` / 1 | sRGB EOTF（Blender "Standard" 视图家族） |
| C. AgXDisplay | `agx` / 2 | 经典 AgX 显示变换（three.js/Filament 变体，rec.2020 原色） |
| D. ExperimentalProxy | — | 未实现（仅当 A/B/C 均失败再做） |

实现位于 `canonical/color_transform.{h,cpp}`，probe 与 bridge **共享同一实现** → 两端 cross-validation 逐位一致。bridge ABI：`NR_FrameDesc.encoding`（v0.2.0-a3）；backend 新增 `clamp_input` 选项（默认关）。

## 验证结果（本机自动验证）

| 项 | 结果 |
|---|---|
| 单元测试 | ✅ **1106 项全过**（sRGB 已知值/单调性；AgX 端点/中灰≈0.22 中性/单调/NaN 安全/确定性；矩阵行和≈1 灰度保持） |
| 回归基线 | ✅ `probe --input 12140.png --clamp` 输出 = A2 基线 `2843dd23…6388da` 逐位一致 |
| HDR 三路径 | ✅ `--hdr-pattern`（含 10×/100× 高光）三编码全部成功，输入/输出哈希各不相同、无 NaN |
| 确定性 | ✅ AgX 路径重跑输出哈希逐位一致（`921fa535…00bd`） |
| EXR bit-exact 桥梁 | ✅ Blender 存 EXR → probe 输入哈希 `b3362b79…` == Blender 内处理输入；输出 = `2843dd23…` 与 PNG 路径一致 |
| 隔离 grep | ✅ 无新增泄漏 |

## AgX 实现说明（重要）

- 系数移植自 **three.js**（MIT）/ **Google Filament**（Apache-2.0）AgX（经典闭式多项式 + 矩阵，见 THIRD_PARTY_NOTICES）。
- **Blender 5.2 查看器的 AgX 是 LUT 变体**（EaryChow fork：ACES2065→E-Gamut 矩阵 + lg2 分配 [-12.47393, 12.526] + `AgX_Base_sRGB.cube` 3D LUT），与闭式经典 AgX 在高光极端处有差异——因此"我们的 AgX 路径输出"与"Blender 查看器的 AgX 显示"不会逐位一致，属预期。本实验的问题是"网络喜欢哪种输入域"，不是"匹配查看器"。
- 中灰（0.18）在 Filament 变体中映射到 ~0.22（比 Sobotka 原版更暗），单测已按此标定。

## 视觉验收（用户执行）

1. 打开 Blender 5.2.1 → 打开 `场景打包.blend` → **F12** 渲染（Cycles，2160×1080）。
2. 控制台重载 addon（若未加载）：`exec(open(r"...blender\addon\__init__.py").read())` + `register()`。
3. 渲染完成后切到 Compositing 工作区一次（让 Viewer Node 更新），控制台执行三条路径并分别查看结果：
   ```python
   bpy.ops.dlss5nr.process_render_result(encoding="0")   # Scene Linear
   bpy.ops.dlss5nr.process_render_result(encoding="1")   # Standard
   bpy.ops.dlss5nr.process_render_result(encoding="2")   # AgX
   ```
   （每次执行会覆盖 `DLSS5_NR_Result`，可先在 Image Editor 切换查看或改名保存对比。）
4. 观察重点：**高光**（灯泡/金属反光是否爆掉或发灰）、**肤色**、**布料纹理**、整体对比度与噪声。
5. 选定默认编码后告诉我，写入 A3.md 与 README，A3 即完成。

## 已知变化（相对 A2）

- 默认行为从"上传钳制 [0,1]"改为"scene-linear HDR 直通"（`--clamp`/`clamp` 属性可恢复旧行为，回归已验证等价）。
- 输出 Image 的 colorspace 标签随编码：SceneLinear→"Linear"，Standard/AgX→"sRGB"。
