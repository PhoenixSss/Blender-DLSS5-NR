# A7 — Blender UI / Preferences / ZIP Packaging

状态：**自动验证通过**（2026-09-05）；GUI 体验验收待用户执行。

## 交付内容

| 文件 | 职责 |
|---|---|
| `blender/__init__.py` | addon 入口：bl_info（§30 免责）、`DLSS5NR_Settings` PropertyGroup（挂 Scene）、`DLSS5NR_Preferences`（runtime 目录/NGX core/GPU/拒绝未签名） |
| `blender/operators.py` | Process Render Result / Process Image Datablock / Copy Runtime Hash + `_run_processing`（读场景参数与偏好设置） |
| `blender/panel.py` | Image Editor 侧栏：主面板（§11 参数 + Session 信息）+ Diagnostics 子面板（§16 全字段，A5 收口） |
| `build.ps1 -Package` | 打包 `bin/blender_dlss5_addon.zip`：6 个 .py + `native/nr_bridge.dll` + `native/nvngx.dll_dlss5.dll`；**§30 断言**：包内禁止 NVIDIA runtime 文件 |
| `blender/bridge.py` | DLL 发现顺序：`<addon>/native/` → env → dev `../bin` |

## 自动验证记录

| 项 | 结果 |
|---|---|
| 构建 + 单测 | ✅ 1109 checks |
| ZIP 结构 | ✅ 顶层目录 `blender_dlss5_addon\`（Blender 约定）+ 8 文件、无 NVIDIA 文件 |
| Headless 安装 | ✅ `addon_install` FINISHED → 启用 → 3 operator / Scene.dlss5nr / 面板 / Preferences 全部注册 |
| **Headless 端到端（已安装 addon）** | ✅ Preferences 设 runtime 目录 → process_image FINISHED → `DLSS5_NR_Result` 3840×2160（包内 DLL 发现生效） |
| Headless 回归（dev 脚本） | ✅ a2_test.py 正常（imports 未破坏） |

## §29 Installation 验收映射

- ZIP 可通过 Blender Preferences 安装 ✅（headless 实测 install+enable）
- 无需 Visual Studio ✅（native DLL 随包分发）
- 包含本项目 bridge 与 shim ✅；**不含 NVIDIA runtime** ✅（脚本断言）
- RTX 50 能进入 capability probe；旧卡拒绝；无兼容模式 ✅（A1/A3 已验证逻辑，随包不变）

## GUI 验收步骤（用户执行）

1. Blender 5.2.1 → **Edit → Preferences → Add-ons → Install from Disk** → 选 `bin\blender_dlss5_addon.zip` → 启用 "Blender DLSS 5 Neural Rendering"。
2. Preferences 面板（同页展开该 addon）→ **Runtime Directory** 设为含 `nvngx_dlssnr.dll` 的目录。
3. 打开 `场景打包.blend` → F12 渲染 → 切 Compositing 工作区。
4. Image Editor 侧栏（N）→ **DLSS 5 NR** 面板 → **Process Render Result**。
5. 验收点：
   - 结果 `DLSS5_NR_Result` 出现、参数（Style/Intensity/Color Input Mode 等）生效；
   - Diagnostics 子面板显示 GPU/driver/runtime 哈希/签名/分类/Create/Evaluate（SHA-256 可点复制）；
   - **F3 搜索可找到 "Process Render Result"**（正式注册路径应解决 A2 的搜索问题）；
   - 错误场景（如 runtime 目录未设）有清晰报错。
6. 结果记录到本文档 → A7 正式验收。

## 已知说明

- 面板固定 Source = Render Result / Combined（§11 v0.1 范围）；Scene/View Layer 选择器留待多场景需求。
- F3 中 operator 名称现在是简洁的 "Process Render Result"（原 "DLSS 5 NR:" 前缀已去掉）。
- 打包脚本 `-Package` 与构建联动：改 C++ 后需重新 `build.ps1 -Package` 才会更新包内 DLL。
